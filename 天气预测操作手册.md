# 天气舒适度预测系统操作手册

## 📋 目录

1. [系统简介](#系统简介)
2. [环境准备](#环境准备)
3. [数据准备](#数据准备)
4. [模型训练](#模型训练)
5. [模型预测](#模型预测)
6. [结果解读](#结果解读)
7. [常见问题](#常见问题)
8. [附录](#附录)

---

## 系统简介

### 功能概述

天气舒适度预测系统是一个基于机器学习的智能预测工具，能够根据天气条件（温度、湿度、气压、风速、降水量）预测人体的舒适度指数。

### 系统特点

- **自动化程度高**：自动识别数据列，自动训练模型
- **预测准确**：R² 分数达到 0.98+，预测精度高
- **操作简便**：命令行操作，简单易用
- **结果直观**：输出包含输入数据和预测结果，便于分析

### 预测指标说明

**舒适度指数（Comfort Index）**：
- 范围：通常为 0-100
- 数值越高，表示天气条件越舒适
- 受温度、湿度、气压、风速、降水量等多个因素综合影响

---

## 环境准备

### 1. 检查 Python 环境

确保系统已安装 Python 3.6 或更高版本：

```bash
python3 --version
```

### 2. 安装依赖包

进入项目目录，安装所需依赖：

```bash
cd /path/to/PredictFlow
pip3 install -r requirements.txt
```

主要依赖包包括：
- pandas：数据处理
- numpy：数值计算
- scikit-learn：机器学习
- joblib：模型保存与加载

### 3. 验证安装

检查关键包是否安装成功：

```bash
python3 -c "import pandas, numpy, sklearn, joblib; print('依赖包安装成功')"
```

---

## 数据准备

### 1. 训练数据格式

训练数据文件应包含以下列：

| 列名 | 说明 | 单位 | 示例值 |
|------|------|------|--------|
| `temperature` | 温度 | 摄氏度 (°C) | 25.8 |
| `humidity` | 湿度 | 百分比 (%) | 65.4 |
| `pressure` | 气压 | 百帕 (hPa) | 1012.8 |
| `wind_speed` | 风速 | 米/秒 (m/s) | 18.9 |
| `precipitation` | 降水量 | 毫米 (mm) | 5.8 |
| `comfort_index` | 舒适度指数 | 0-100 | 68.9 |

**文件位置**：`data/train/weather_data.csv`

**数据要求**：
- CSV 格式，UTF-8 编码
- 第一行为列名
- 数据行为数值，无缺失值
- 建议至少 30 条以上数据

### 2. 预测数据格式

预测数据文件只需包含输入特征列（不包含 `comfort_index`）：

| 列名 | 说明 | 单位 | 示例值 |
|------|------|------|--------|
| `temperature` | 温度 | 摄氏度 (°C) | 19.5 |
| `humidity` | 湿度 | 百分比 (%) | 58.2 |
| `pressure` | 气压 | 百帕 (hPa) | 1012.3 |
| `wind_speed` | 风速 | 米/秒 (m/s) | 14.5 |
| `precipitation` | 降水量 | 毫米 (mm) | 3.2 |

**文件位置**：`data/input/new_weather_data_for_prediction.csv`

**数据要求**：
- CSV 格式，UTF-8 编码
- 第一行为列名
- 列名必须与训练数据中的输入列名完全一致
- 列的顺序可以不同，系统会自动调整

### 3. 数据示例

**训练数据示例**（`data/train/weather_data.csv`）：
```csv
temperature,humidity,pressure,wind_speed,precipitation,comfort_index
15.2,45.3,1013.5,8.2,0.0,78.5
18.5,52.1,1012.8,12.5,0.0,75.3
22.3,58.7,1011.2,15.8,0.0,72.1
...
```

**预测数据示例**（`data/input/new_weather_data_for_prediction.csv`）：
```csv
temperature,humidity,pressure,wind_speed,precipitation
19.5,58.2,1012.3,14.5,3.2
26.8,67.5,1010.8,19.7,8.9
23.1,61.3,1011.5,16.2,5.4
...
```

---

## 模型训练

### 1. 训练命令

使用以下命令训练天气舒适度预测模型：

```bash
python3 scripts/inspect_and_train.py \
    data/train/weather_data.csv \
    --inputs temperature,humidity,pressure,wind_speed,precipitation \
    --outputs comfort_index \
    --auto \
    --out-model models/weather_model.joblib
```

### 2. 参数说明

| 参数 | 说明 | 必需 | 示例 |
|------|------|------|------|
| `data/train/weather_data.csv` | 训练数据文件路径 | ✓ | - |
| `--inputs` | 输入特征列名（逗号分隔） | ✓ | `temperature,humidity,pressure,wind_speed,precipitation` |
| `--outputs` | 输出目标列名（逗号分隔） | ✓ | `comfort_index` |
| `--auto` | 自动模式，无需交互确认 | ✗ | - |
| `--out-model` | 模型保存路径 | ✗ | `models/weather_model.joblib`（默认） |

### 3. 训练过程输出

训练过程中会显示以下信息：

```
正在加载数据文件: data/train/weather_data.csv
已加载数据，行数=50, 列数=6

=== 列信息 ===
  0. temperature 	 类型=float64 	 缺失值=0
  1. humidity 	 类型=float64 	 缺失值=0
  2. pressure 	 类型=float64 	 缺失值=0
  3. wind_speed 	 类型=float64 	 缺失值=0
  4. precipitation 	 类型=float64 	 缺失值=0
  5. comfort_index 	 类型=float64 	 缺失值=0

=== 数值列统计信息 ===
               count      mean        std       min       25%       50%       75%       max
temperature     50.0    25.808   6.233741     14.8     21.125    25.750    30.125     38.6
humidity        50.0    65.654  11.610977     43.6     57.900    65.550    73.650     88.2
...

============================================================
最终选定的输入列： ['temperature', 'humidity', 'pressure', 'wind_speed', 'precipitation']
最终选定的输出列： ['comfort_index']
============================================================

开始训练模型...
训练集样本数：40
测试集样本数：10
输入特征数：5
输出目标数：1
训练完成！

=== 模型评估结果 ===
R2 分数: 0.9833
平均绝对误差 (MAE): 0.6597

模型已保存到: models/weather_model.joblib
```

### 4. 模型评估指标说明

- **R² 分数（决定系数）**：
  - 范围：0-1，越接近 1 越好
  - 0.98+ 表示模型拟合度很高
  - 说明：模型能解释 98%+ 的数据变异

- **MAE（平均绝对误差）**：
  - 单位：与目标变量相同（舒适度指数）
  - 数值越小越好
  - 说明：预测值与真实值的平均偏差

### 5. 训练完成检查

训练成功后，检查模型文件是否生成：

```bash
ls -lh models/weather_model.joblib
```

---

## 模型预测

### 1. 从文件批量预测

使用训练好的模型对新的天气数据进行预测：

```bash
python3 scripts/predict.py \
    --model models/weather_model.joblib \
    --input data/input/new_weather_data_for_prediction.csv \
    --output output/weather_predictions.csv
```

### 2. 参数说明

| 参数 | 说明 | 必需 | 示例 |
|------|------|------|------|
| `--model` | 模型文件路径 | ✓ | `models/weather_model.joblib` |
| `--input` | 输入数据文件路径 | ✓* | `data/input/new_weather_data_for_prediction.csv` |
| `--output` | 预测结果保存路径 | ✗ | `output/weather_predictions.csv` |
| `--interactive` | 交互式预测模式 | ✓* | - |

*注：`--input` 和 `--interactive` 必须选择其一

### 3. 预测过程输出

```
已加载模型: models/weather_model.joblib
输入列: ['temperature', 'humidity', 'pressure', 'wind_speed', 'precipitation']
输出列: ['comfort_index']

正在读取输入文件: data/input/new_weather_data_for_prediction.csv
读取到 5 条数据

输入数据预览:
   temperature  humidity  pressure  wind_speed  precipitation
0         19.5      58.2    1012.3        14.5            3.2
1         26.8      67.5    1010.8        19.7            8.9
2         23.1      61.3    1011.5        16.2            5.4
3         31.2      75.8    1009.2        22.6           12.3
4         28.4      69.6    1010.1        20.3            9.7

正在进行预测...

预测结果预览:
   temperature  humidity  pressure  wind_speed  precipitation  comfort_index
0         19.5      58.2    1012.3        14.5            3.2        72.6430
1         26.8      67.5    1010.8        19.7            8.9        68.0980
2         23.1      61.3    1011.5        16.2            5.4        70.4580
3         31.2      75.8    1009.2        22.6           12.3        65.1640
4         28.4      69.6    1010.1        20.3            9.7        66.8255

预测结果已保存到: output/weather_predictions.csv
```

### 4. 交互式预测

如果需要单条数据实时预测，可以使用交互式模式：

```bash
python3 scripts/predict.py \
    --model models/weather_model.joblib \
    --interactive
```

然后按提示依次输入各个特征值：

```
=== 交互式预测模式 ===
请按提示输入数据，输入 'q' 退出

------------------------------------------------------------
请输入 temperature: 25.5
请输入 humidity: 60.0
请输入 pressure: 1012.0
请输入 wind_speed: 15.0
请输入 precipitation: 5.0

预测结果:
  comfort_index: 70.2345
```

---

## 结果解读

### 1. 预测结果文件格式

预测结果文件（`output/weather_predictions.csv`）包含原始输入数据和预测结果：

```csv
temperature,humidity,pressure,wind_speed,precipitation,comfort_index
19.5,58.2,1012.3,14.5,3.2,72.6430
26.8,67.5,1010.8,19.7,8.9,68.0980
23.1,61.3,1011.5,16.2,5.4,70.4580
31.2,75.8,1009.2,22.6,12.3,65.1640
28.4,69.6,1010.1,20.3,9.7,66.8255
```

### 2. 结果分析示例

以实际预测结果为例：

| 序号 | 温度 | 湿度 | 气压 | 风速 | 降水量 | 舒适度指数 | 分析 |
|------|------|------|------|------|--------|------------|------|
| 1 | 19.5°C | 58.2% | 1012.3 hPa | 14.5 m/s | 3.2 mm | **72.64** | 温度适中，湿度较低，舒适度较高 |
| 2 | 26.8°C | 67.5% | 1010.8 hPa | 19.7 m/s | 8.9 mm | **68.10** | 温度较高，有降水，舒适度中等 |
| 3 | 23.1°C | 61.3% | 1011.5 hPa | 16.2 m/s | 5.4 mm | **70.46** | 温度适宜，各项指标均衡 |
| 4 | 31.2°C | 75.8% | 1009.2 hPa | 22.6 m/s | 12.3 mm | **65.16** | 高温高湿，降水量大，舒适度较低 |
| 5 | 28.4°C | 69.6% | 1010.1 hPa | 20.3 m/s | 9.7 mm | **66.83** | 温度偏高，有降水，舒适度中等偏低 |

### 3. 舒适度指数参考

| 舒适度指数范围 | 舒适程度 | 说明 |
|---------------|---------|------|
| 80-100 | 非常舒适 | 天气条件极佳，适合户外活动 |
| 70-80 | 舒适 | 天气条件良好，体感舒适 |
| 60-70 | 一般 | 天气条件尚可，部分人群可能感觉不适 |
| 50-60 | 不舒适 | 天气条件较差，建议减少户外活动 |
| 0-50 | 极不舒适 | 天气条件恶劣，不建议户外活动 |

### 4. 影响因素分析

各天气因素对舒适度的影响：

- **温度**：适宜温度范围（18-26°C）舒适度较高
- **湿度**：湿度适中（40-60%）最舒适，过高或过低都会降低舒适度
- **气压**：正常气压范围（1010-1015 hPa）影响较小
- **风速**：微风（5-15 m/s）有助于散热，提高舒适度
- **降水量**：无降水或小雨（<5 mm）舒适度较高，大雨会显著降低舒适度

---

## 常见问题

### Q1: 训练时提示"文件不存在"？

**A**: 检查文件路径是否正确，确保：
- 文件路径使用相对路径或绝对路径
- 文件名大小写正确
- 文件确实存在于指定位置

```bash
# 检查文件是否存在
ls -l data/train/weather_data.csv
```

### Q2: 预测时提示"缺少输入列"？

**A**: 检查预测数据文件的列名是否与训练数据一致：
- 列名必须完全匹配（区分大小写）
- 必须包含所有输入特征列
- 可以使用以下命令检查列名：

```bash
head -1 data/input/new_weather_data_for_prediction.csv
```

### Q3: 模型评估分数很低（R² < 0.8）？

**A**: 可能的原因和解决方法：
- **数据质量问题**：检查数据是否有异常值、缺失值过多
- **数据量不足**：增加训练数据量（建议至少 50 条以上）
- **特征选择不当**：确认输入特征与输出目标确实相关
- **数据分布问题**：检查数据是否覆盖了足够的范围

### Q4: 预测结果不合理（如舒适度指数超出正常范围）？

**A**: 检查：
- 输入数据是否在训练数据的合理范围内
- 是否有异常值（如温度超过 50°C 或低于 -20°C）
- 单位是否正确（如温度单位是摄氏度而非华氏度）

### Q5: 如何更新模型？

**A**: 使用新的训练数据重新训练模型：

```bash
# 1. 准备新的训练数据（包含更多样本）
# 2. 重新训练模型（会覆盖旧模型）
python3 scripts/inspect_and_train.py \
    data/train/weather_data_updated.csv \
    --inputs temperature,humidity,pressure,wind_speed,precipitation \
    --outputs comfort_index \
    --auto \
    --out-model models/weather_model.joblib
```

### Q6: 可以预测多个目标变量吗？

**A**: 可以。修改训练命令，指定多个输出列：

```bash
python3 scripts/inspect_and_train.py \
    data/train/weather_data.csv \
    --inputs temperature,humidity,pressure \
    --outputs comfort_index,uv_index,air_quality \
    --auto \
    --out-model models/weather_multi_model.joblib
```

### Q7: 支持 Excel 格式的数据文件吗？

**A**: 支持。可以直接使用 `.xlsx` 或 `.xls` 文件：

```bash
python3 scripts/inspect_and_train.py data/train/weather_data.xlsx --auto
python3 scripts/predict.py --model models/weather_model.joblib --input data/input/prediction.xlsx
```

---

## 附录

### A. 完整操作流程示例

**步骤 1：准备训练数据**
```bash
# 将训练数据保存到 data/train/weather_data.csv
```

**步骤 2：训练模型**
```bash
python3 scripts/inspect_and_train.py \
    data/train/weather_data.csv \
    --inputs temperature,humidity,pressure,wind_speed,precipitation \
    --outputs comfort_index \
    --auto \
    --out-model models/weather_model.joblib
```

**步骤 3：准备预测数据**
```bash
# 将待预测数据保存到 data/input/new_weather_data_for_prediction.csv
```

**步骤 4：执行预测**
```bash
python3 scripts/predict.py \
    --model models/weather_model.joblib \
    --input data/input/new_weather_data_for_prediction.csv \
    --output output/weather_predictions.csv
```

**步骤 5：查看结果**
```bash
cat output/weather_predictions.csv
```

### B. 文件结构说明

```
PredictFlow/
├── data/
│   ├── train/
│   │   └── weather_data.csv          # 训练数据
│   └── input/
│       └── new_weather_data_for_prediction.csv  # 预测数据
├── models/
│   └── weather_model.joblib          # 训练好的模型
├── output/
│   └── weather_predictions.csv        # 预测结果
├── scripts/
│   ├── inspect_and_train.py          # 训练脚本
│   └── predict.py                    # 预测脚本
└── 天气预测操作手册.md                # 本操作手册
```

### C. 快速参考命令

```bash
# 训练模型（自动模式）
python3 scripts/inspect_and_train.py data/train/weather_data.csv \
    --inputs temperature,humidity,pressure,wind_speed,precipitation \
    --outputs comfort_index --auto --out-model models/weather_model.joblib

# 批量预测
python3 scripts/predict.py --model models/weather_model.joblib \
    --input data/input/new_weather_data_for_prediction.csv \
    --output output/weather_predictions.csv

# 交互式预测
python3 scripts/predict.py --model models/weather_model.joblib --interactive
```

### D. 联系与支持

如遇到问题或需要帮助，请：
1. 查看本文档的"常见问题"部分
2. 检查项目 README.md 文件
3. 查看训练和预测脚本的详细输出信息

---

**文档版本**：v1.0  
**最后更新**：2024年  
**适用系统**：PredictFlow 天气预测系统

